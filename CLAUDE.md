# CLAUDE.md — CubeNest (단일 자립 명세)

## 개요

- **CubeNest(큐브네스트):** 사고력수학부터 초등교과까지 다루는 '쌓기나무(공간과 입체)' 3D 학습 웹 도구 — 3D 회전, 위·앞·옆 투상, 개수·부피·겉넓이 자동 계산, 블록 추가·삭제.
- 태블릿·휴대폰 우선. 무료 런칭(트래픽·피드백) 단계. 개인사업자·정적 호스팅.
- 사이트맵: `/`(landing) · `/playground`(현행 3D 도구) · `/worksheets`(PDF 문제은행) · `/quiz`(자동채점 퀴즈) · `/guide`(사용방법)

## 불변 규칙 (전 페이지 공유·재정의 금지)

- 데이터 모델: 3D 격자 `(x,y,z)` 채움/비움, 채운 칸 1 = 나무 1. 축: **앞=+z, 옆(오른쪽)=+x, 위=+y.**
- **중력:** 뜬 나무 비허용. 추가 시 뜨는 자리 차단, 받침 나무 삭제 차단(낙하 없음).
- 격자: 기본 3³, 축별 독립, **최대 10.** 밖으로 나가는 축소는 차단.
- **투영: 전체 직교.** 편집은 **자유 3D 보기에서만**, 위·앞·옆은 관찰 전용(카메라 이동만).

## 모양 수학 (계산·문제의 단일 기준)

- **직렬화(F2):** `[버전,gx,gy,gz,edge] + 채운 칸 비트마스크`(셀 index `x·gy·gz+y·gz+z`) → URL-safe base64 `?m=`. **모양만 담고 문제 메타 금지.**
- **값(정확식):** 부피 = 개수×모서리³ / 맞닿은 면 쌍 = 인접(+x·+y·+z) 채운 쌍 수 / **노출면 = 6×개수 − 2×맞닿은 면 쌍**(항상 정확) / 겉넓이 = 노출면×모서리² (**바닥 포함**).
  - 동치 표기: 노출면 = `2×(위+앞+옆)`, 여기서 위·앞·옆 = **방향별 '드러난 면 수'**(실루엣 아님). **UI는 이 형태로 표기**하고 count−pairs 형태식은 UI에 안 보인다.
- **투상 2종:** ① 높이 지도(각 칸 층수 → 개수 유일) ② 세 방향 실루엣(투영 그림자 → 최소·최대 발생). ※①②의 '위앞옆'은 서로 다른 개념.
- **역방향(세 실루엣 → 모양), 중력 준수:** 모양 = 높이 지도 `h(x,z)`. **최대** `h = min(앞높이[x], 옆높이[z])`. **최소** = 앞·옆 봉우리를 최소 칸으로 실현(작은 덮개, **정확 계산, 값 유일**). **은면(숨은 나무) = 최대 − 최소.**
- **출제 범위:** quiz·worksheet는 **연결된(구멍 없는) 모양만** 출제.
- **문제 스키마(F5):** `{shape, givens, ask, answer, reveal}` — quiz·worksheet·mission 공유.
- **공유 기반 요소(재사용, 재발명 금지):** F1 `track()` 계측 · F2 직렬화 · F3 이미지 캡처 · F4 정답 노출 상태 · F5 문제 스키마.

## playground (현행 구현 — 세부는 코드가 기준)

- 추가/삭제 토글(기본 추가): 추가 = 바닥/면 탭(뜨는 자리·격자 밖 차단), 삭제 = 나무 탭(받침이면 차단). 되돌리기·다시하기·전체 지우기.
- 위·앞·옆 버튼 = 카메라 이동(직교 유지), 회전하면 자유 복귀('자유' 버튼 없음). 나침반·방향 라벨(위=파랑·앞=초록·옆=빨강). 칸별 개수 토글(기본 off, 빌보드).
- 층별 관찰: 다중 선택(트리거 + 체크리스트 팝오버), 실제 쌓인 층만, 빈 선택 → 전체 복귀.
- 줌: 돋보기 ±·핀치·스크롤(fit 버튼 없음). 격자 조절: 설정 시트(프리셋 3³·4³ + 축별 스텝퍼, 모서리 1~10cm).
- 계산 표시: 우상단 통계 바 → 탭하면 우측 계산 패널(3줄: 답/숫자식/한국어식, 초등용 곱 표기).
- 공유 시트(링크·PNG 워터마크·네이티브 링크 공유) / 의견(구글폼 + 오류 자동 미리채움) / GA4 opt-in 동의 배너.
- 모델 프레이밍: UI 제외 '콘텐츠 사각형'에 fit + 오프셋 직교 투영, 경계구 기준(회전해도 안정).

## 브랜드 · 디자인 토큰

- **단일 소스:** `assets/css/tokens.css`(`:root` 변수) — `index.html`·`playground/index.html`·`guide`·`quiz`·`worksheets` 공용. **새 색상은 여기 추가, 페이지별 하드코딩 금지.**
- **색상:**
  - 배경: `--bg:#eef2f7` / 패널: `--panel:#ffffff` · `--panel-2:#f7f9fc`
  - 텍스트: `--ink:#182233`(본문) · `--muted:#6a7789` · `--muted-2:#8a95a6`(보조)
  - 라인: `--line:#e5eaf1` · `--line-2:#eef2f7`
  - **핵심 컬러(accent, 인디고):** `--accent:#3f3fbf` · `--accent-ink:#2d2d95`(강조 텍스트/hover) · `--accent-soft:#ecebfb`(옅은 배경)
  - 추가(초록): `--add:#0f9d58` / `--add-soft:#e6f5ec` — playground 추가 모드·"위" 방향 라벨(파랑은 별도 하드코딩, 나침반 한정)
  - 삭제(빨강): `--del:#e0466b` / `--del-soft:#fce9ee` — playground 삭제 모드
  - 방향 라벨(나침반, 토큰 아님): 위=파랑·앞=초록·옆=빨강 (`playground` 세부 규칙 참조)
- **그림자:** `--shadow`(기본 카드) · `--shadow-sm`(버튼/토글) · `--shadow-lg`(시트/모달) — 모두 `--ink` 기반 반투명(`rgba(24,34,51,*)`), 새 그림자 추가 시 이 톤 유지.
- **라운드:** `--r:16px`(기본) · `--r-lg:22px`(시트/모달/큰 카드).
- **폰트:** `--font` = Pretendard(웹폰트) → 시스템 산세리프(Apple SD Gothic Neo·Malgun Gothic·system-ui) 순 폴백. 커스텀 폰트 추가 금지, 전부 이 변수 사용.
- **로고/글리프:** `site-brand .glyph`(헤더 로고) 그림자는 톤 다름(`rgba(160,110,50,.28)`, 우드/큐브 색감) — 나무 큐브 모티프 유지, 배경 원색은 accent 톤 침범 금지.

## 기술 · 규약

- **단일 HTML + Three.js, 빌드 없음, 정적 호스팅**(GitHub Pages). 로컬 확인: `python -m http.server`.
- 렌더링: 직교 카메라 1개, 큐브 **InstancedMesh 2그룹**(선택 불투명 / 흐림 반투명) + 병합 모서리 → 드로우콜 격자 무관 ~4.
- **모바일 가로 우선(playground only):** 세로 시 앱 90° 회전(입력 좌표 변환). 버튼 44px+, 하단 dock, hover 미사용.
- 언어 한국어. 브랜드: `navigator.language` ko → '큐브네스트' / else 'CubeNest'.
- 계측: `track(name, params)` → GA4, **동의 후에만**(동의 영구 / 거부 24h 만료).

## 데이터 · 인증 · 수익화

- **login-free-first:** landing·playground·worksheets·quiz는 정적/클라이언트로 완결(DB 불필요). 저장은 **로컬 우선**, `save/load`를 얇게 추상화(나중에 클라우드로 교체).
- **`/tutor`(AI 선생님)만** LLM 키를 숨기는 **얇은 서버 프록시**(익명 + IP 한도), 로그인과 분리.
- 수익화(학부모 B2C): **관리형 결제로 얇게** 시작, 무료로 수요 검증 후 도입. DB는 결제·계정 필요 시점에.
- 아동 개인정보: **학생 무로그인**, 계정은 **성인(교사·학부모) 중심**, 아동 PII 최소화.

## 로드맵

- **P0 완료:** GA4·동의, 상태 공유(URL·이미지), 의견 보내기.
- **P1(재사용 순서):** 정답 숨기기(F4) → 퀴즈(F5·역방향) → 미션 → 워크시트 PDF(F3·F5).
- 새 기능은 **F1~F5 · 역방향 최소·최대 엔진**을 재사용한다.
